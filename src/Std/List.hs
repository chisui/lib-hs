module Std.List
    ( module Base
    ) where

import "base" Data.List as Base
    ( intersperse, intercalate, transpose, subsequences, permutations
    , scanl, scanl', scanl1
    , scanr, scanr1
    , iterate, iterate'
    )
